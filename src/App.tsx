import { Layout, Header, BoardContainer } from "./lib/components";

const App = () => {
  return (
    <Layout>
      <Header />
      <BoardContainer>-</BoardContainer>
    </Layout>
  );
};

export default App;
